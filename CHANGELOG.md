# 更新紀錄

格式依照 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版本號依照[語意化版本](https://semver.org/lang/zh-TW/)。
每一版的下載檔在 [Releases](https://github.com/leon80148/icope_tool/releases)。

## [Unreleased]

### 變更
- **新年度的計畫頁面還沒開放（或代碼命名改了）時，不再被當成登入失效。** 以前每查一次就重新登入一次、還顯示「可能有其他人正用同一個帳號登入」；現在找不到查詢頁的計畫會標成「找不到查詢頁」，判定只看有回答的計畫，全部找不到時說明可能是新年度尚未開放並指引到國健署網站或設定。一次查詢最多只復原一次（先重建 context、再沿用共用 cookie、最後才登入），不會把其他電腦踢下線。
- 計畫代碼的命名規則只寫在一個地方；設定頁的年度標籤改用注入的時鐘，程式跨年開著不關也會換成新年度。
- 維護文件新增「每年 1 月的檢查清單」（`docs/hpdcs_protocol.md`）。

## [1.1.1] - 2026-09-18

### 修正
- 發佈的 zip 改用符合規格的正斜線路徑。1.1.0 的 zip 用 Windows 檔案總管「解壓縮全部」沒有問題，但部分第三方解壓工具會把它解成檔名含反斜線的檔案。

## [1.1.0] - 2026-09-18

第一個公開發佈的版本。

### 新增
- **院所預設**：轉介資源與衛教單張可以設為「院所預設」。製作轉介單時勾選異常項目，按「帶入院所預設」就一次帶入；按鈕會先寫出這次帶入哪些項目，取消勾選項目時它帶入的內容一起取消。日常印一份最少 6 次點擊。
- **確認與列印捷徑**：第一、二步就能直接跳到最後一步預覽後列印。
- **只印衛教單張**：不加轉介單首頁、不需要國健署帳號，也不會動到做到一半的轉介單。輸出前會重新核對 PDF 內容，別台電腦剛換過檔案時不會印出舊的。
- **各項衛教重點**：每個異常項目可以寫一段院所自己的衛教文字（200 字、6 行以內），印在轉介單該項目的標題下方。
- **年齡核對**：讀健保卡查詢時，結果上方顯示年度年齡（今年度滿 65 歲；未滿時提醒原住民 55 歲以上可評估）。只是提醒，不改變國健署的判定；出生日期只在記憶體。
- **圖文並茂的使用說明**：程式內按 F1，以及 [docs/GUIDE.md](docs/GUIDE.md) 的操作教學，共 16 張標了編號的畫面；最後有「卡住了怎麼辦」。
- **院所設定包工具** `tools/make_clinic_pack.py`：把院所自己整理的清單（JSON）與 PDF 做成可以匯入的設定包；清單打錯會直接報錯，不會做出內容不對的設定包。
- 設定包會帶著「院所預設」標記；自我檢測新增使用說明圖片的檢查。

### 變更
- 名稱改為「**ICOPE 小幫手**」。程式檔名 `IcopeTool.exe` 與本機設定資料夾 `%LOCALAPPDATA%\IcopeTool` 不變，已安裝的電腦直接覆蓋更新即可。
- 用語統一為「衛教單張」；首次設定清單改成「用得到的再設定」，只印衛教單張不必先設定國健署帳號。
- 採用 MIT 授權。

### 修正
- 轉介單 PDF：比一頁還高的資源區塊不再整塊被推到下一頁、留下幾乎空白的一頁。
- 只印衛教單張：背景核對期間別台電腦停用或調整了單張時，不再輸出過期的合併檔；在系統列印視窗按取消之後，「重試列印」仍然有作用。
- 使用說明（F1）裡每張圖片下方多出的大片空白。

### 安全性
- 共用資料夾裡的衛教單張清單只接受單張資料夾內的 PDF 檔名：被竄改的清單無法讓其他電腦開啟或刪除資料夾外的檔案。
- 匯入設定包前先檢查 zip 宣告的解壓後大小（清單 8 MB、每份單張 60 MB、合計 400 MB），不會被壓縮炸彈塞滿記憶體。

## [1.0.0] - 2026-09-17

院所內部使用的第一個版本，沒有公開發佈。

### 新增
- **篩檢查詢**：讀健保卡或輸入身分證字號，自動登入國健署 hpdcs 查詢今年是否已做 ICOPE；驗證碼自動辨識（辨識不出時人工輸入）；帳號或密碼錯誤時只嘗試一次並暫停自動登入，避免帳號被鎖。
- **轉介衛教單精靈**：8 個評估項目 → 轉介資源 → 衛教單張 → 預覽、列印或另存 PDF（A4、大字）。
- 轉介資源庫（從衛生局「社區資源盤點表」Excel 匯入、匯入範本、常用與停用）、衛教單張管理（PDF 拖放加入、排序、遺失檔案重新指定）。
- 設定包匯出／匯入；多台電腦共用同一個資料夾（NAS 或網路磁碟）；`--self-test` 自我檢測。
