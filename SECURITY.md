# 安全性

## 回報安全問題

請不要在公開的 Issue 裡貼出可以被利用的細節。請用 GitHub 的私下回報：
**[Security › Report a vulnerability](https://github.com/leon80148/icope_tool/security/advisories/new)**。

回報時請附上版本號（主畫面左下角）、重現步驟，以及你認為的影響範圍。**不要附上真實的身分證字號、姓名、國健署帳號密碼或健保卡資料**；用 `A123456789` 這類測試值即可。

只有最新發佈的版本會收到修正。

## 這個程式怎麼處理敏感資料

| 資料 | 存在哪裡 | 說明 |
|---|---|---|
| 國健署帳號密碼、登入 cookie | 資料存放位置的 `auth/`（明文 JSON） | 多台電腦要共用同一組帳密，單機加密（DPAPI）別台解不開；安全邊界是資料夾的存取權限。不會出現在畫面、紀錄、錯誤訊息或設定包。 |
| 查詢過的身分證字號、姓名、出生日期 | 只在記憶體 | 關閉程式即清除；出生日期只跟著讀卡當下的身分證。 |
| 稽核紀錄 | `%LOCALAPPDATA%\IcopeTool\logs\audit.log` | 只記事件與身分證的 SHA-256 前 8 碼。 |
| 產生的轉介單 PDF（含長者姓名） | 本機暫存資料夾 `%TEMP%\IcopeTool` | 超過一天自動清除，不放在共用資料夾。 |
| 轉介資源、衛教單張、診所資訊 | 資料存放位置 | 不含個資；設定包只帶資源與單張。 |

## 設計上的防護

- **防鎖帳號**：登入失敗時只有國健署明確回覆「驗證碼錯誤」才重試，其他失敗立刻停止並暫停自動登入。
- **網路**：只連 `https://hpdcs.hpa.gov.tw`（驗證憑證、每個請求都有逾時）。沒有自動更新、不回傳使用統計，onnxruntime 的遙測事件已關閉。
- **匯入的檔案視為不可信任**：設定包不採用包內的檔名，匯入前依 zip 宣告的大小設限（清單 8 MB、每份單張 60 MB、合計 400 MB）；Excel 以唯讀、只取值的方式讀取。
- **共用資料夾**：衛教單張清單裡的檔名只接受單張資料夾內的 PDF 檔名，被竄改的清單會讀取失敗，而不是讓其他電腦開啟或刪除別的檔案。
- **發佈**：由 GitHub Actions 從 tag 建置，Release 頁面附 SHA-256。exe 沒有程式碼簽章。

## 已知的限制

- 能寫入資料存放位置的人，就能讀到國健署帳密，也能替換衛教單張的 PDF。請用 Windows／NAS 的權限保護這個資料夾。
- 程式以使用者自己的國健署帳號自動登入並辨識驗證碼；是否符合貴單位與主管機關的使用規範，請自行評估。
